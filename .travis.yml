language: node_js
node_js:
  - 7
cache:
  yarn: true
env:
  global:
  - NODE_ENV=test
services:
- mongodb
- rabbitmq
- redis-server
install:
- pwd
- cd ./api && npm install
- pwd
- cd ./worker && npm install
- pwd
cache:
  directories:
    - ./api/node_modules
    - ./worker/node_modules
before_script:
- sleep 10
script:
- pwd
- cd ./api && npm test
- pwd
